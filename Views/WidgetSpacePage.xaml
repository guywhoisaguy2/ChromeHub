<UserControl x:Class="ChromeHub.Views.WidgetSpacePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        
        <!-- Widget Canvas -->
        <ScrollViewer Grid.Column="0" Background="{DynamicResource CanvasBrush}">
            <Canvas x:Name="WidgetCanvas" Background="Transparent" Height="2000" Width="1000">
                <ItemsControl ItemsSource="{Binding Widgets}" Loaded="OnCanvasLoaded">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas Background="Transparent"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding X}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="WidgetBorder" Width="{Binding Width}" Height="{Binding Height}" 
                                    Background="{DynamicResource WidgetBrush}" CornerRadius="12" 
                                    BorderBrush="{DynamicResource WidgetBorderBrush}" BorderThickness="1"
                                    MouseDown="OnWidgetMouseDown" MouseMove="OnWidgetMouseMove" MouseUp="OnWidgetMouseUp"
                                    Cursor="Hand" AllowDrop="True">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Widget Header -->
                                    <Border Grid.Row="0" Background="{DynamicResource WidgetHeaderBrush}" CornerRadius="12,12,0,0" Padding="15,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Icon}" FontSize="20" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="✕" Click="OnRemoveWidget" Style="{DynamicResource CloseButtonStyle}" Margin="5,0,0,0" Tag="{Binding}"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- Widget Content -->
                                    <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="{DynamicResource TextBrush}" Margin="15" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    
                                    <!-- Widget Footer -->
                                    <Border Grid.Row="2" Background="{DynamicResource WidgetFooterBrush}" CornerRadius="0,0,12,12" Padding="15,10">
                                        <TextBlock Text="Added: {Binding AddedDate, StringFormat=g}" FontSize="10" Foreground="{DynamicResource SubtextBrush}"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </ScrollViewer>
        
        <!-- Available Widgets Panel -->
        <Border Grid.Column="1" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,0,0">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Available Widgets" FontSize="16" FontWeight="Bold" Margin="15,15,15,10" Foreground="{DynamicResource TextBrush}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding AvailableWidgets}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource WidgetBrush}" CornerRadius="8" Margin="10,5" Padding="10" BorderBrush="{DynamicResource WidgetBorderBrush}" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Orientation="Vertical">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{DynamicResource SubtextBrush}"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="+" Click="OnAddWidget" Style="{DynamicResource AddButtonStyle}" Tag="{Binding}" Margin="5,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>